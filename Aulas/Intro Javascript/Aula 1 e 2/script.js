alert("Tudo ok");


 //VARIÁVEIS

     // Tipos primitivos:

        //boolean (VERDADEIRO OU FALSO)
       /* var vOUf = false;
        console.log(vOUf);
        console.log(typeof(vOUf)); //Typeoff é para identificar o tipo de variável.

        //number
        var numeroqualquer = 1;
        console.log(numeroqualquer);
        console.log(typeof(numeroqualquer)); 

        // string
        var nome = "Diana";
        console.log(nome);
        console.log(typeof(nome)); 

        //Escopos

     var escopoGlobal = 'Global';
     console.log(escopoGlobal);

     function escopoLocal () {
         let escopoLocalInterno = 'Local';
         console.log(escopoLocalInterno)
     }    
          //Uma let só é encontrada onde foi utilizada, se colocassemos o log fora da função não iria encontrá-la.
          escopoLocal();  

        //Atribuições

        var atribuicao ='diana';
        var comparacao = '0' == 0;
        console.log(comparacao);
        var comparacaoIdentica = '0' === 0;
        console.log(comparacaoIdentica); //Nesse caso é falso, pois '' transforma em string. São valores iguais com tipos diferentes.

        //Operadores Aritméticos
 
        //adição 
        var adicao = 1 + 1;
        //subtração
        var subtracao = 2 - 1;
        //multiplicação 
        var mult = 2 * 3;
        //divisão Real
        var dReal = 6 / 2;
        //Divisão inteira (Ou resto da divisão)
        var dInt 5 % 2;
        //potenciação (2³ por exemplo)
        var potenciacao = 2 ** 3;

        


     

